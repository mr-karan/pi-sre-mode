---
description: Start a queue or broker investigation
---
Investigate broker or queue issues affecting `$1` over `${@:2}`.

Focus on:
- backlog or pending work
- slow consumers or disconnects
- producer versus consumer imbalance
- downstream services that may be victims instead of causes
